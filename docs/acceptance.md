# Текущее состояние приёмки

Дата: 23.09.2026. Ветка `main`. Live-проверка выполнялась с локальным `.env`; сам файл исключён из Git.

| Проверка | Состояние | Основание |
|---|---|---|
| M1: карточка и наличие | live пройдено | `npm run live:check`: страница EKT из 20 товаров и detail; индекс 25 страниц, 500 карточек. Цена, остаток и время перечитываются из API. |
| M2: доступный аналог отсутствующего товара | live пройдено с оговоркой | `18157` (`310100086_`), 0 шт. → `515262` (`200300265_`), 74 шт. на 23.09.2026 11:06 UTC. Совпали 3 полюса, 20 А, 400 В, 4,5 кА и характеристика C. Монтажная совместимость и аксессуары не подтверждены API. |
| M3: условия покупки | пройдено по официальной странице | [Полезная информация EKT](https://ekt.kz/about/information/), проверено 23.09.2026; минимальная партия неизвестна. |
| M4: подтверждаемое добавление | live HTTP и браузер пройдены | Корзина пуста до согласия; после подтверждения позиция сохранена. Повтор не дублирует, другая сессия не видит товар. Тестовая позиция удалена. |
| M5: ссылка на корзину | live браузер пройден | `/cart` показывает позицию той же сессии после refresh. |
| OpenAI Responses | live пройдено | `gpt-6-sol` выполнил цикл вызова `get_product_details`; инструмент корзины вызван также через чат приложения. |
| XLSX/XLS, DOCX, текстовый PDF | demo-тесты пройдены | Синтетические документы; XLSX и PDF дополнительно прошли HTTP-сценарий. |
| JPEG и скан PDF | функция реализована; live OCR не проверен | Реальные файлы для проверки не предоставлены. |
| Сертификат реального товара | не подтверждён | В проверенной паре ссылки на сертификат не найдено; ветка UI проверена fixture-ом. |
| Docker Compose | не проверен | Docker Engine недоступен на рабочей машине. |

Проверки: `npm run typecheck`, `npm run lint`, `npm test` (22 теста), `npm run build`, `npm run live:check`, `npm run catalog:sync`, `npm run analogs:discover`, `npm run smoke:live`, `npm run browser:live`. Последние две команды требуют запущенного live-сервера на `http://localhost:3000`; браузерная проверка использует установленный Microsoft Edge. Скриншоты: `screenshot-live-desktop.png` и `screenshot-live-mobile.png`. Демо-проверки запускаются отдельно при `DATA_MODE=demo`.

Остатки и цены могут измениться. Перед демонстрацией перечитайте карточки или запустите проверочные команды заново. Индекс из 500 товаров неполный; отсутствие кандидата в нём не доказывает отсутствие товара в магазине. Корзина приложения собственная: заказ, резерв и запись товаров на ekt.kz не выполняются.