services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/app.sqlite
    volumes:
      - ekt_data:/app/data
volumes:
  ekt_data:
